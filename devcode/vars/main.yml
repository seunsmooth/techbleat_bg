---
# vars file for web
  vars:
    - ansible_user: ubuntu
    - ansible_ssh_private_key_file: /Users/oawoga/play/redis_blue_green/olukey      
    - ansible_python_interpreter: /usr/bin/python3
    - ansible_host: "{{ public_ip_address }}"
  